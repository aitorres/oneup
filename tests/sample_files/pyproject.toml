[tool.poetry.dependencies]
requests = "^2.26.0"
toml = "^0.10.2"

[tool.poetry.dev-dependencies]
pytest = "^6.2.5"
pytest-cov = "^3.0.0"
flake8 = "^4.0.1"

[tool.poetry.group.test.dependencies]
Django = "^4.2.1"
django-stubs = "^1.7.0"
