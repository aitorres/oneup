[project]
name = "test"
version = "0.1.0"
requires-python = ">=3.12, <3.14"
dependencies = [
    "tweepy (>=4.16.0,<5.0.0)",
    "requests (>=2.32.5,<3.0.0)",
    "atproto (>=0.0.57,<0.0.58)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
